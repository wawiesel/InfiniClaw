# `upsertSystemRecord`

Reads `systems.json`, merges one system record, and optionally writes the result back.

This satisfies the beacon requirement to register a system in fleet public state during bootstrap.

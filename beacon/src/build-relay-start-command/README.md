# `buildRelayStartCommand`

Builds the exact relay start command beacon hands off after bootstrap.

This keeps the beacon/relay split explicit: beacon bootstraps the system, relay becomes the room-facing runtime.

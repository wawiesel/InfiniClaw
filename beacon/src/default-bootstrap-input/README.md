# `defaultBootstrapInput`

Normalizes partial bootstrap input into a complete beacon bootstrap request.

This provides the concrete defaults needed for a system-level install path without burying them in the CLI.

# `validateInput`

Checks that beacon bootstrap has enough information and that the local working-copy directories exist.

This satisfies the bootstrap requirement that beacon installation fail early on invalid system bootstrap input.

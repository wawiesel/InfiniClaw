{
  "allowedRoots": [
    {
      "path": "~",
      "allowReadWrite": false,
      "description": "Home directory — read-only access (excludes dotfiles)"
    },
    {
      "path": "~/_vault",
      "allowReadWrite": true,
      "bots": ["commander"],
      "description": "Vault — Johnny5's read-write workspace"
    },
    {
      "path": "~/2026-Nanoclaw/InfiniClaw/bots/profiles/commander",
      "allowReadWrite": true,
      "bots": ["commander"],
      "description": "Commander credentials dir — workspace-mcp reads credentials and writes tokens here"
    },
    {
      "path": "~/2026-Nanoclaw/InfiniClaw",
      "allowReadWrite": true,
      "description": "InfiniClaw repo — Cid's workspace"
    },
    {
      "path": "~/2026-Nanoclaw/InfiniClaw/_runtime",
      "allowReadWrite": false,
      "description": "Runtime data — read-only (instances, staging, logs)"
    }
  ],
  "blockedPatterns": [".*"],
  "nonMainReadOnly": false
}

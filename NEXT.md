# NEXT — Future Work

- Rename supervisor to "relay" (or similar) — the current name conflicts with the concept of a process supervisor (pm2). The relay is a lightweight Matrix message relay + command handler, not a process manager.
